{#
| Variable         | Type    | Description                     |
|------------------|---------|---------------------------------|
| cart             | object  |                                 |
| cart.items_count | int     |                                 |
| cart.total       | string  | Formatted total ex: "١٠٠ ر.س"   |
#}
{% set important_links = theme.settings.get('important_links') %}
{% set show_topnav = theme.settings.get('show_topnav', true) %}

{# Advanced header icon colour (behind a toggle, footer-style). Emitted as a CSS
   variable only when set; header.scss falls back to the palette text colour, so
   defaults and any already-set colours are untouched. The dark value overrides
   the same variable under `.dark` (gated on dark_mode_enabled in the editor),
   so a single-palette store just uses the normal colour. #}
{% set hc_advanced  = theme.settings.get('header_advanced_colors') ?? false %}
{% set hc_icon      = hc_advanced ? theme.settings.get('header_icons_color', '') : '' %}
{% set hc_icon_dark = hc_advanced ? theme.settings.get('header_icons_color_dark', '') : '' %}
{% if hc_icon or hc_icon_dark %}
<style>
  {% if hc_icon %}.store-header { --header-icon-color: {{ hc_icon }}; }{% endif %}
  {% if hc_icon_dark %}.dark .store-header { --header-icon-color: {{ hc_icon_dark }}; }{% endif %}
</style>
{% endif %}
<header class="store-header {{ theme.settings.get('header_icons_border', true) ? '' : 'header-icons--no-border' }} {{ theme.settings.get('hide_search_icon', false) ? 'header--search-hidden' : '' }}">
  {# Top Nav #}
  {% if show_topnav %}
  <div  class="top-navbar  bg-light-background dark:bg-dark-background  dynamic-text-color">
    <div class="container flex justify-between">
      <div class="flex-1 flex items-center gap-2">
          {# Footer Menu #}
          {% if important_links %}
            <salla-menu source="footer" topnav></salla-menu>
          {% endif %}

          {# Language & Currency Component #}
          <div class="header-buttons">
              {% if store.settings.is_multilingual or store.settings.currencies_enabled %}
                   <salla-localization-modal show-trigger></salla-localization-modal>
              {% endif %}
          </div>

          {# Scopes | Branches #}
          {% if store.scope %}
              <button class="btn--rounded-gray"
                      onclick="salla.event.dispatch('scopes::open', {'mode': 'default'})">
                  <i class="sicon-location rtl:ml-1 ltr:mr-1"></i> {{ store.scope.name }}
              </button>
          {% endif %}

          {# Search bar — a trigger for the global <salla-search> in master.twig rather than
             its own instance, styled to match the inline search field it replaces. #}
          <div class="header-search flex-1">
            <button type="button"
                    class="relative flex items-center w-full min-w-[220px] h-9 rounded-md bg-white border border-gray-100 text-sm text-gray-500 transition-colors hover:border-gray-200 rtl:pr-10 rtl:pl-3 ltr:pl-10 ltr:pr-3"
                    aria-label="{{ trans('blocks.footer.search') }}"
                    onclick="salla.event.dispatch('search::open')">
              <i class="sicon-search absolute rtl:right-3 ltr:left-3 text-base text-gray-400"></i>
              <span>{{ trans('blocks.footer.search') }}</span>
            </button>
          </div>
      </div>

      <salla-contacts is-header></salla-contacts>
    </div>
  </div>
  {% endif %}

  {# Main Nav #}
  <div id="mainnav" class="main-nav-container   dynamic-bg-color !font-thin shadow-default ">
      <div class="inner   bg-inherit">
          {% set header_layout = theme.settings.get('header_layout', 'centered_menu') %}
          {% set is_drawer_layout = header_layout == 'centered_logo_drawer' %}
          {# centered_logo_drawer: the logo is absolutely centred, so on small screens it
             can overlap the right-side icons. Pull the content nearer the edges on mobile
             and tag it so header.scss can tighten the icon cluster and drop the search
             icon below `lg`. #}
          <div class="{{ is_drawer_layout ? 'mx-2 lg:mx-10 header--centered-drawer' : 'mx-10' }} header-content">
              <div class="flex items-stretch justify-between relative {{ is_drawer_layout ? 'min-h-[68px] lg:min-h-[84px]' : '' }}">
                  <div class="flex items-center">
                      {% if is_drawer_layout %}
                        {# Layout 3: our custom drawer menu replaces the native mobile menu at all widths #}
                        <volt-drawer-menu></volt-drawer-menu>
                      {% else %}
                        {# Layouts 1 & 2: native hamburger handles mobile, horizontal menu shows on desktop #}
                        <a class="lg:hidden mburger mburger--collapse leading-none rtl:ml-4 ltr:mr-4" href="#mobile-menu" aria-label="Open-menu">
                          <i class="hover:text-[var(--color-primary)] sicon-menu text-primary text-2xl"></i>
                        </a>
                      {% endif %}

                      {# Logo — inline at the start for the horizontal-menu layouts #}
                      {% if not is_drawer_layout %}
                        <a class="navbar-brand" href="{{ store.url }}">
                            <img fetchpriority="high" width="100%" height="100%" loading="eager" src="{{ store.logo }}" alt="{{ store.name }} logo">
                            {% if is_page('index') %}
                              <h1 class="sr-only">{{ store.name }}</h1>
                            {% else %}
                              <h2 class="sr-only">{{ store.name }}</h2>
                            {% endif %}
                        </a>
                      {% endif %}

                      {# Layout 2: horizontal menu sits right next to the logo #}
                      {% if header_layout == 'menu_beside_logo' %}
                        <custom-main-menu></custom-main-menu>
                      {% endif %}
                  </div>

                  {# Layout 1 (default): horizontal menu centered between logo and icons #}
                  {% if header_layout == 'centered_menu' %}
                    <custom-main-menu></custom-main-menu>
                  {% endif %}

                  {# Layout 3: logo absolutely centered in the bar #}
                  {% if is_drawer_layout %}
                    <a class="navbar-brand absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2" href="{{ store.url }}">
                        <img fetchpriority="high" width="100%" height="100%" loading="eager" src="{{ store.logo }}" alt="{{ store.name }} logo">
                        {% if is_page('index') %}
                          <h1 class="sr-only">{{ store.name }}</h1>
                        {% else %}
                          <h2 class="sr-only">{{ store.name }}</h2>
                        {% endif %}
                    </a>
                  {% endif %}

                  <div class="flex items-center  justify-end">


                  {# Always render the user component; with `show-trigger` it shows the
                     account menu when logged in and a login trigger for guests
                     (upstream theme-raed#866), replacing the old user/guest branch. #}
                  <salla-user-menu avatar-only show-header show-trigger></salla-user-menu>
                    {% if theme.settings.get('dark_mode_enabled', true) %}
                      <button
                        id="theme-toggle"
                        class="header-btn   "
                        type="button"
                        aria-pressed="false"
                        aria-label="Toggle color mode"
                      >
                        <i class="hover:text-[var(--color-primary)] header-btn__icon icon sicon-moon hidden dynamic-text-color" data-theme-icon="dark" aria-hidden="true" style="display:none"></i>

                        <i class="hover:text-[var(--color-primary)] header-btn__icon icon sicon-sun dynamic-text-color" data-theme-icon="light" aria-hidden="true"></i>
                        <span class="sr-only">Toggle color mode</span>
                      </button>
                      {% endif %}


                            {# Opens the global <salla-search> in master.twig, so search also works
                               inside the Salla mobile app (which hides the theme header/footer). #}
                            {% if not theme.settings.get('hide_search_icon', false) %}
                            <button class="   dynamic-text-color"
                              id="search-btn"
                              type="button"
                              aria-label="{{ trans('blocks.footer.search') }}"
                              onclick="salla.event.dispatch('search::open')">
                            <i class="hover:text-[var(--color-primary)] header-btn__icon sicon-search dynamic-text-color"></i>
                          </button>
                          {% endif %}


                    <salla-cart-summary class="ml-4 rtl:ml-[unset] rtl:mr-4 dynamic-text-color">
                      {#  #}
                        <i slot="icon" class=" hover:text-[var(--color-primary)] header-btn__icon icon sicon-shopping-bag dynamic-text-color"></i>
                      </salla-cart-summary>


                  </div>
              </div>
          </div>
      </div>
  </div>
  <span class="navbar-underline"></span>
</header>

{# The theme's own search modal was removed: the global <salla-search> in master.twig
   provides its own modal, opened via salla.event.dispatch('search::open'). #}