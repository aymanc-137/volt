<!-- To eliminate all render blocking CSS & JS and start with the critical css-->
<!-- To show loader to the user till the page content are  loaded-->
<style>
  .loader-init {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: white;
      transition: opacity 0.75s, visibility 0.75s;
      z-index: 9999;
      overflow: hidden;
  }

  .loader-init--hidden {
      opacity: 0;
      visibility: hidden;
  }

  /* Default spinner — shown when no logo is configured */
  .loader-init::after {
      content: "";
      width: 36px;
      height: 36px;
      border: 3px solid #eee;
      border-top-color: #5bd5c4;
      border-radius: 50%;
      animation: loading 0.75s ease infinite;
      position: relative;
      z-index: 1;
  }
  /* Hide the spinner when a logo is shown instead */
  .loader-init--has-logo::after { content: none; }

  /* Store logo */
  .loader-logo {
      width: auto;
      height: auto;
      max-width: 140px;
      max-height: 140px;
      object-fit: contain;
      position: relative;
      z-index: 1;
  }

  /* Blurred background: soft glowing blobs using the store's theme colors */
  .loader-blur-bg {
      position: absolute;
      inset: 0;
      z-index: 0;
      overflow: hidden;
  }
  .loader-blur-bg::before,
  .loader-blur-bg::after {
      content: "";
      position: absolute;
      width: 45vmax;
      height: 45vmax;
      border-radius: 50%;
      filter: blur(90px);
      opacity: 0.55;
  }
  .loader-blur-bg::before {
      background: var(--color-primary, #5bd5c4);
      top: -12%;
      inset-inline-start: -12%;
  }
  .loader-blur-bg::after {
      background: var(--color-scandary, #FF3B3B);
      bottom: -12%;
      inset-inline-end: -12%;
  }

  @keyframes loading {
      from {
        transform: rotate(0turn);
      }
      to {
        transform: rotate(1turn);
      }
  }
</style>
<script>
  window.addEventListener("load", () => {
    const loader = document.querySelector(".loader-init");
    loader?.classList.add("loader-init--hidden");
    loader?.addEventListener("transitionend", () => {
  if (!document.querySelector(".loader-init")) return;
      document.body.removeChild(loader);
      });
  });
</script>